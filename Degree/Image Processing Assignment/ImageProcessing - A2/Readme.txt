This pratical compile with python virtual env 
install with command: 
1) pip install virtualenv
2) pip venv env
To use it's library:
cd venv\Scripts\activate

To fullfill submission requirement measure, detectCorner, detectObject
of python file is driven by main.py, to run use: python main.py 
